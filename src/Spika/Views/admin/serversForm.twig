{% extends "admin/adminBase.twig" %}

{% block content %}

<script type="text/javascript">
    
    $(document).ready(function() {

        $('#chkbox_change_password').click(function() {
        
            var $this = $(this);

            if ($this.is(':checked')) {
                
                $('#group_password').removeAttr("disabled");
                $('#group_password').val('');
                
            } else {
                
                $('#group_password').attr("disabled","disabled");
                $('#group_password').val('');
                
            }
            
        });        
    });
    
</script>

            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">
                        {% if mode == 'new' %}
                            {{lang.labelAddServer}}
                        {% elseif mode == 'view' %}
                            {{lang.labelViewServer}}
                        {% elseif mode == 'edit' %}
                            {{lang.labelEditServer}}
                        {% endif %}                    
                    </h1>
                </div>
            </div>
             
            <div class="row">
                <div class="col-lg-12">
                    
                    {% include "admin/alerts.twig" %}
                    
                    {% if mode == 'edit' %}
                        <form role="form" method="post" action="{{ROOT_URL}}/admin/servers/edit/{{id}}" enctype="multipart/form-data">
                    {% else %}
                        <form role="form" method="post" action="{{ROOT_URL}}/admin/servers/add" enctype="multipart/form-data">
                    {% endif %}  

                        <div class="form-group">
                            <label>{{lang.formServerName}}</label>
                                {% if mode == 'new' or mode == 'edit' %}<span class="text-danger">{{lang.noteRequired}}</span>{% endif %}
                            
                            {% if mode == 'new' or mode == 'edit'%}
                                <input class="form-control" name="name" value="{{formValues.name}}">
                            {% else %}
                                <div class="well">{{formValues.name}}</div>
                            {% endif %}
                            
                        </div>
                        
                        <div class="form-group">
                            <label>{{lang.formServerUrl}}</label>
                                {% if mode == 'new' or mode == 'edit' %}<span class="text-danger">{{lang.noteRequired}}</span>{% endif %}
                            
                            {% if mode == 'new' or mode == 'edit'%}
                                <input class="form-control" name="url" value="{{formValues.url}}">
                            {% else %}
                                <div class="well">{{formValues.url}}</div>
                            {% endif %}
                            
                        </div>
                            
                        {% if mode == 'new' or mode == 'edit' %}
                            <button type="submit" class="btn btn-primary btn-lg btn-block">{{lang.btnSave}}</button>
                        {% else %}

                        {% endif %}
                        
                    </form>
                    
                </div>
                
            </div>
             
</script>
       
{% endblock %}
